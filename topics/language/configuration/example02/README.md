# Example 02

Changing into the `some-module` directory and then executing Terraform will cause `some-module` to be the root module and there will be no child modules.

```sh
cd some-module
terraform init
terraform plan
terraform apply
```
