# Example 01

This example shows a resource with the following information.

- The resource type is `null_resource`.
- The resource name is `example_app`.
- The resource has an attribute of `triggers`.
