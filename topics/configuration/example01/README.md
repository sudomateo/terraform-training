# Example 01

Executing Terraform from this directory will cause this directory to be the root module and `some-module` to be a child module (that we aren't calling).

```sh
terraform init
terraform plan
terraform apply
```
